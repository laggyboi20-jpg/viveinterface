org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric — 1.21.4. Mappings: official Mojang (build.gradle).
minecraft_version=1.21.4
loom_version=1.16.3
loader_version=0.19.3

# Fabric API
fabric_version=0.119.4+1.21.4

# Mod Menu + Cloth Config (compile-only; optional at runtime — drive the settings screen)
modmenu_version=13.0.4
cloth_config_version=17.0.144

# Mod
mod_version=1.0.0-beta
maven_group=com.laggy
archives_base_name=viveinterface

# Path to a Vivecraft 1.21.4 production jar (sibling folder on this machine).
# Loom remaps it (intermediary -> mojmap) automatically via modCompileOnly.
# Drop the jar at this path, or repoint this line to wherever it lives.
vivecraft_jar=../Useful Stuff Like sister Mods+/vivecraft-1.21.4-1.3.15-fabric.jar
